# Site settings
# Accessed via site.metadata.title, site.metadata.email, and so on.

title: Software Architecture Insights
tagline: Architecture thinking you can actually use. Every Tuesday morning.
author: Lee Atchison
email: lee@leeatchison.com
description: >-
  Software Architecture Insights is Lee Atchison's weekly newsletter on
  software architecture, cloud computing, and building systems that scale —
  practical thinking you can actually use.

# Hand-maintained; Lee bumps it when it moves (Spec0024 Open Question 13).
subscriber_count: "1,700+"

# Kit account details this site depends on but does not own. Both are
# Kit-side setup Lee does by hand (Spec0024's Netlify/DNS boundary) — this
# file just holds the resulting values so nothing is hard-coded in templates.
#
# kit_inline_form_uid: the uid of the Kit inline subscribe form (Open
# Question 14) — "Basic Form", created 2026-09-03, embedded on the home
# page, the subscribe page, and at the foot of every article.
kit_inline_form_uid: "c448363077"
# Fathom site ID for this domain (Open Question 14) — the same ID every
# other site in this repo uses (one Fathom property covers all of Lee's
# sites; confirmed 2026-09-03).
fathom_site_id: "QZJQFDMY"

# The Kit hosted-landing-page uids that were here (the diagnostic and
# webinar forms, plus the old subscribe-page uid) are gone: confirmed
# 2026-09-03 via Kit's API that all three are `type: "hosted"` with
# `embed_js: null`, not embeddable script-tag forms the way
# kit_inline_form_uid above is. Rather than ship dead embeds,
# src/service-ownership-diagnostic.erb and src/architecting-with-ai.erb
# were removed at Lee's direction; netlify.toml now redirects both paths
# straight to Kit's still-working hosted pages (temporary, see the
# redirect rules' own comment). Rebuilding these as real Bridgetown pages
# later needs matching embed-type Kit forms first.
