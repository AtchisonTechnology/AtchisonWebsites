---
layout: default
---

<% articles = site.collections.articles.resources.select { |r| Array(r.data.categories).include?(data.title) } %>

<header class="listing-header">
  <h1><%= data.title %></h1>
  <p class="listing-description"><%= data.description %></p>
</header>

<ul class="article-list">
  <% articles.each do |article| %>
    <li class="article-list-item">
      <a href="<%= relative_url article.relative_url %>"><%= article.data.title %></a>
      <p class="article-list-description"><%= article.data.description %></p>
    </li>
  <% end %>
</ul>
